package model;

/**
 * Represents the possible values of a cell on the Tic-Tac-Toe board.
 */
public enum Cell {
    /** The cell is empty. */
    EMPTY,
    /** The cell contains an X (human player). */
    X,
    /** The cell contains an O (AI player). */
    O
}
